# 飞书论文笔记写作规范

> 本文档仅作作者自用规范参考，不发布到博客站点。
> 同步脚本：[scripts/sync-feishu.js](scripts/sync-feishu.js)

---

## 1. 总览

博客的「论文库 / 论文详情页」全部由飞书文档自动生成。作者在飞书写好之后，每次 `git push` 到 `main` 分支，或在仓库 Actions 中手动触发 **Sync Feishu Papers**，脚本会：

1. 读取飞书文档内容
2. 按结构拆成 N 篇论文
3. 生成 `papers/paper-001.html` … `paper-NNN.html`
4. 更新 `posts.json`，首页、论文库、标签过滤会立刻生效

数据链路：
```
飞书 Wiki / Docx 文档
   → sync-feishu.js（GitHub Actions 中运行）
      → papers/paper-*.html  （独立论文页）
      → posts.json            （首页/论文库 共用数据）
```

---

## 2. 文档层级（必须遵守）

同步脚本**完全依赖飞书标题的「块类型（block_type）」识别结构**，不是靠加粗或者字号。在飞书写的时候，必须选工具栏里的「标题」样式。

| 飞书中的样式 | 块类型编号 | 脚本识别为 | 说明 |
|---|---|---|---|
| 一级标题（H1，`#`） | 3 | — 忽略 | 文档总标题，不会写入任何论文正文 |
| 二级标题（H2，`##`） | 4 | **分类名（source）** | 论文库左侧分类栏按这个聚合。<br>前缀「一、二、三…」会被自动剥离。 |
| 三级标题（H3，`###`） | 5 | **一篇新论文的开始（title）** | 每出现一个 H3，就「结束上一篇、开启新一篇」。<br>标题里不要含「阅读统计 / 推荐 / 推荐阅读 / 进度」，否则会被跳过。 |
| 四级标题（H4，`####`） | 6 | 论文正文 `<h4>` | 论文内部小节标题，如「核心发现」「实验范式」 |
| 五级标题（H5，`#####`） | 7 | 论文正文 `<h5>` | 论文内部子小节 |
| 正文段落 | 1 / 2 | `<p>` | 默认写法 |
| 引用（blockquote） | 12 | `<blockquote>` | 用来写结论性的重要引用或金句 |
| 任务列表（待办） | 13 | `☐ xxx`（todo-item） | 尽量少用，建议改用普通列表 |
| 无序列表（bullet） | 9 | `• xxx`（list-item） | 核心发现、待跟进问题、方法细节，推荐用它 |
| 有序列表（ordered） | 10 | `1. xxx` 样式（list-item） | 步骤、流程类内容 |
| 分割线（divider） | 22 | `<hr />` | 两篇论文之间可加分割线，视觉清晰 |

---

## 3. 一篇论文的标准结构（推荐模板）

在 H3 标题之后，**立即放元信息列表**，再写正文。不要把正文插在元信息之前。

```
### <论文主标题>
- 作者：<姓名>, <姓名>, <姓名>
- 发表年份：<YYYY>
- 期刊：<期刊名>
- DOI：<10.xxxx/xxxxx>
- 原文链接：<URL>（可选）

<第一段：摘要 / 一句话总览>

#### 核心发现
- 要点 1
- 要点 2
- 要点 3

> 最关键的结论，用引用块写一句「论文的最大贡献 / 我认为最重要的 takeaway」。

#### 实验范式 / 方法细节
- 被试 / 模型 / 数据集
- 关键变量定义
- 对照设计

#### 主要结果
<段落描述或列表>

#### 不足 / 局限
- <列出可改进之处>

#### 待跟进问题
- <想继续读的相关论文、想做的复现实验、想验证的假设>
```

### 3.1 字段说明

- **标题（H3）**：
  - 建议用中文主标题，英文术语保留原词即可
  - 脚本会自动把前缀 `论文 N | ` 这种样式去掉
  - 严禁包含 `阅读统计`、`推荐`、`推荐阅读`、`进度` 这些词，会被当作管理栏目跳过

- **作者**：写「中文名, 英文名」或「姓名, 姓名」都可以，用无序列表 `-` 格式即可
- **年份**：必填，后续可能做时间排序
- **期刊 / DOI**：非强制，但建议补全方便回查原文
- **第一段摘要**：`posts.json` 里摘要会自动从**第一段正文**里截取前 100 个汉字，所以第一段尽量写成"一句话把工作讲清楚"的风格

---

## 4. 分类组织建议（H2）

H2 是分类，也就是论文库左侧「按期刊分类」的筛选项。建议按**期刊大类 + 自建主题类**混合组织，例如：

- `Nature Neuroscience`
- `Nature Reviews Neuroscience`
- `Nature`（主刊）
- `Science`
- `Cell`
- `Neuron`
- `Reviews 综述`（所有非特定期刊的综述放这里）
- `理论与计算`（贝叶斯、主动推断、RNN 模型类）
- `方法学`（记录方法、分析工具）
- `读书会 / 阅读组讨论`（读书会的笔记也可以放）

同一个分类下的论文在最终页面中会被聚在一起，分类顺序就是飞书文档里 H2 出现的顺序。

---

## 5. 多份文档怎么配（进阶）

`FEISHU_DOC_URL` 支持写多个链接，用英文逗号 `,`、分号 `;` 或换行分隔即可：

```
FEISHU_DOC_URL =
  https://xxx.feishu.cn/wiki/TokenA,
  https://xxx.feishu.cn/wiki/TokenB,
  https://xxx.feishu.cn/docx/TokenC
```

脚本会按链接顺序逐个读取，并把所有论文**合并成一个论文库**，生成 `paper-001` 到 `paper-NNN`。

> **建议**：同一个分类尽量放在同一份文档里，避免两份文档都定义了同名 H2（虽然合并没问题，但不方便日后维护）。

---

## 6. 同步后最终产物长什么样

### 6.1 首页（index.html）
- 从 `posts.json` 读取，论文会出现在"论文笔记"区域
- 摘要就是从论文第一段正文截取的 100 字

### 6.2 论文库（notes.html）
- 左侧分类栏 = 文档中所有 H2（去重）
- 点分类 → 过滤出对应分类的论文卡片
- 点卡片 → 打开 `papers/paper-xxx.html`

### 6.3 论文详情（papers/paper-xxx.html）
- 顶部：标题 + 当前是第 N 篇 / 共 M 篇（上下篇导航）
- 左侧：目录（自动从正文的 H4/H5 生成）
- 正文：按块渲染，引用 / 列表 / 分割线保留样式
- 底部：返回顶部按钮

---

## 7. 常见坑 & 排查

| 症状 | 原因 | 修复 |
|---|---|---|
| 同步报错「未找到任何论文」 | 文档确实是空的，或 H3 被写成加粗文本而不是"标题"样式 | 在飞书确认每条论文标题是真正的 H3（工具栏显示"三级标题"） |
| 分类下没论文 | 论文的 H3 写在 H2 之前，或者 H2 / H3 用反了（分类用了 H3） | 保证顺序：先 H2，再 H3，再正文 |
| 一篇论文包含了下一篇的内容 | 两篇论文之间缺一个 H3，或者第二篇标题写成 H4 | 每篇论文前用**独立的 H3** 开头 |
| 摘要为空 | 第一段全是列表 / todo / 短行，没有一段超过 10 字的正文段落 | 在元信息后面写一句话摘要 |
| 页面目录没内容 | 正文里没有任何 H4/H5 | 给「核心发现」「实验范式」等加上 H4 |
| 出现 `[x] 已读` / `[ ] 未读` 奇怪的行 | 你用飞书的"待办"写了阅读状态 | 保持即可，脚本已经自动跳过这行了 |
| Actions 跑失败，提示 `resource deleted` / `99991668` | `FEISHU_DOC_URL` 里的 token 失效了（旧文档删了 / 迁移了 / token 换了） | 重新从飞书复制文档链接，更新 Secrets |
| Wiki 链接报错 `wiki 节点类型为 XXX` | 这个 Wiki 条目下面挂的不是 docx（可能是旧版 docs / sheet / base） | 把内容迁移到一个新版 docx，或者把 Wiki 节点类型换成 docx |

---

## 8. 本地调试方法（可选）

如果你想在自己电脑上先跑一遍同步确认结果：

```bash
export FEISHU_APP_ID=cli_xxxxxxxxxxxxx
export FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxx
export FEISHU_DOC_URL="https://xxx.feishu.cn/wiki/GM7SwNAA1iAcPzkISSectLcDnSf"
# 多个链接的话用英文逗号分隔：
# export FEISHU_DOC_URL="https://xxx.feishu.cn/wiki/AAAA,https://xxx.feishu.cn/docx/BBBB"

node scripts/sync-feishu.js
```

跑成功后会在控制台看到：
```
  ✓ paper-001.html — 小脑编码时间统计先验知识
  ✓ posts.json 已更新：保留 1 篇，新增/更新 2 篇
```

然后本地用浏览器打开 `notes.html` 或 `papers/paper-001.html` 看效果。

---

## 9. 示例：完整的一份飞书文档（可直接粘贴）

```
# Blog

## Nature Neuroscience

### 小脑编码时间统计先验知识
- 作者：张三, 李四, 王五
- 发表年份：2024
- 期刊：Nature Neuroscience
- DOI：10.1038/s41593-024-00001

本文研究了小脑皮层如何通过学习时序统计结构来形成对未来事件的预测。研究者在小鼠的延迟眨眼行为范式中，通过系统性地改变刺激间隔的概率分布，发现眨眼反应的时间精度与先验分布的方差高度相关。

#### 核心发现
- Purkinje 细胞的简单峰发放相位随训练分布的中心时刻对齐
- 下橄榄核的复杂峰活动编码了预测误差而非绝对时间
- 去除视觉反馈后，小鼠仍能保留统计学习的时间结构

> 这篇工作的最大贡献在于：第一次把贝叶斯最优估计与小脑内部模块的相位关系做了单细胞级别的因果验证。

#### 实验范式
延迟眨眼条件反射 + 头部固定硅探针记录。

#### 待跟进问题
- 双峰分布刺激间隔下，预测是否也会呈现双模态？

---

## Reviews 综述

### 预测编码与主动推断：从理论到神经实现
- 作者：赵六, 孙七
- 发表年份：2025
- 期刊：Nature Reviews Neuroscience
- DOI：10.1038/s41583-025-00002

综述整理了过去十年预测编码（Predictive Coding）和主动推断（Active Inference）框架在系统神经科学中的应用进展。

#### 核心理论对比
- 预测编码：自上而下预测 vs 自下而上预测误差最小化
- 主动推断：加上行为层面的自由能最小化
- 两者在视觉、前额叶、小脑都有证据，但缺乏跨尺度直接比对
```

---

## 10. 参考文件

- 同步脚本实现：[scripts/sync-feishu.js](scripts/sync-feishu.js)
  - 块类型到 HTML：`blockToHtml()`（约 109 行）
  - H2/H3 识别论文结构：`parsePapers()`（约 127 行）
  - Wiki → docx token 解析：`wikiToDocxToken()`（约 45 行）
  - 多文档 URL 拆分：`splitDocUrls()`（约 20 行）
  - 生成 posts.json（保留旧文章）：`updatePostsJson()`（约 283 行）
- GitHub Actions 工作流：[.github/workflows/sync-feishu.yml](.github/workflows/sync-feishu.yml)
- 论文页模板在同步脚本的 `generatePaperHTML()` 函数中

---

*文档版本 v1 — 仅作者自用，不定期随同步脚本迭代*
