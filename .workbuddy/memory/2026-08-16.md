# 2026-08-16 · Blog 工作日志

## 19:00 — 修复论文 TOC 卡片样式
- 用户反馈：博客论文详情页（junnyshang.online/papers/paper-001.html）目录卡样式异常，截图显示只有第一条高亮、其余条目几乎不可见。
- 排查：本地的 styles.css 中 `.article-toc li a` 没有显式 opacity/font-weight，依赖默认色；也没有覆盖 `:visited` 等伪类。Edge 本地渲染正常，但用户 Safari + 部署版本表现异常，怀疑是部署 CSS 与本地不同步，或被某种未预期的样式覆盖。
- 修复（styles.css 第 1869 行附近 `/* ===== 论文详情页目录 ===== */`）：
  - `.article-toc li a` 显式 `color: #111111 !important`、`opacity: 1 !important`、`-webkit-text-fill-color: #111111`、`font-weight: 500`、`font-size: 0.95rem`
  - 增加 `padding: 9px 14px`，让卡片更宽松
  - 单独补齐 `:link/:visited/:focus/:active` 四态 + `:focus-visible` 可见焦点环
  - hover 增加 `transform: translateX(2px)` 微动效
  - 同步加强 dark mode（`@media (prefers-color-scheme: dark)` 与 `body[data-theme="dark"]` 两套）
- 缓存破坏：papers/paper-001..014.html 把 `styles.css?v=40` 改为 `?v=41`，确保部署后走新 CSS。
- 待用户：把新的 styles.css + papers/*.html 部署到线上后验证；如线上仍有问题，需要把部署版的 styles.css 贴出来对比。

## 19:25 — 全站完善扫描 + 快赢修复
- 扫描发现的问题与快赢修复（均已落地）：
  1. CSS 版本统一：根目录 12 个页面 `styles.css?v=40` → `?v=41`（与 papers 对齐，共 26 页全部 v=41）。
  2. 新增 `robots.txt`（Disallow admin/sync/import，指向 sitemap）+ `sitemap.xml`（23 个 URL：7 个页面 + 16 篇文章）。
  3. 重建 `feed.xml`：域名从 zhilinofficial.github.io 改为 junnyshang.online，补齐 14 篇论文笔记（原来只有 2 篇日常），按日期倒序，lastBuildDate 更新。
  4. `posts.json`：补齐 id=1、id=2 两篇日常文章的空 summary；重写为 UTF-8 明文（60KB→28KB，内容完整校验通过：16 条、body 全在）。
  5. index/library/notes/daily/about 五页补 og:image(avatar.jpg)/og:url/og:site_name + twitter card。
- 遗留待办（需用户决策）：
  - ⚠️ admin.js 第 6 行硬编码 DEFAULT_PASSWORD='zhilinOfficial2026'，纯前端校验，部署公开可绕过；GitHub PAT 也在前端处理。
  - paper 详情页 og 标签为静态 shell（article.html 同理），JS 渲染页面 SEO 弱，可考虑预渲染或动态改 meta。
  - dark-preview.html 缺 viewport（预览页，影响小）。

## 19:28 — 四项遗留待办全部落地
1. **admin 安全加固（admin.js）**：
   - 明文 `DEFAULT_PASSWORD` 改为 SHA-256 哈希常量 `PASSWORD_HASH`，登录走 `crypto.subtle` 异步哈希比对；删除 `PASSWORD_KEY` 自定义密码路径。
   - 登录态 AUTH_KEY 与 GitHub Token 从 localStorage 改为 sessionStorage（Token 读取保留 localStorage 兜底以兼容旧数据）。
   - admin.html / sync.html / import.html 头部加 `<meta name="robots" content="noindex, nofollow">`。
   - 注意：仍是前端校验，彻底方案是部署时排除这三个文件（已在 robots.txt Disallow）。
2. **论文页 og 标签**：paper-001..014 静态补 og:type=article / og:title / og:description / og:url / og:image / twitter card（标题取自各页 <title>）。中途插入位置出过 bug（description 自闭合被挤掉），已用正则修复并全量通过 HTML 结构校验。
3. **dark-preview.html** 补 viewport。
4. **search.html 全局搜索**加 150ms debounce（notes.html 论文搜索原本就有）。
- 验证：node --check 两个 JS 通过；16 个 HTML 结构校验通过。

## 19:50 — 功能完善扫描 + 修复死功能
- 发现并修复的真 bug：
  1. **countapi.xyz 已停服**（2023 年下线）：全站访客计数器 + 文章阅读量都调它，一直静默失败（try/catch 吞掉，UI 显示空）。
     - 移除访客计数器整段 IIFE + 孤立的 `.visitor-count` CSS。
     - 文章阅读量改为 localStorage 本地计数，诚实标注「本文你已阅读 N 次」，永不失效、零依赖。
  2. **JS 版本号从未升级**：script.js / page-transition.js / admin.js 全站还是 ?v=40，导致之前所有 JS 改动（含本次 debounce、动态 meta）都不会刷新缓存。全部升到 ?v=41（7 个 script.js + 26 个 page-transition.js + 1 个 admin.js 引用）。
  3. **article.html 动态 SEO**：renderArticle 里补 document.title + og:title/description/url 动态更新，日常文章（article.html?id=N）分享时显示正确标题而非通用「文章详情」。
- 验证：node --check 通过；countapi 引用清零；外部依赖只剩本地 posts.json fetch。
- 遗留可选项（未动）：若想要真实全站访客统计，需接 GoatCounter/Umami 等需注册的隐私分析服务。

## 20:10 — 功能完善第二轮（TOC/灯箱/快捷键/进度/统计）
从高优先级清单推进（跳过真实访客统计），落地 5 项 + 修 1 个暗坑：
1. **日常文章目录 TOC**：article.html 已有空容器 #articleToc 但 renderArticle 从未填充（半成品）。renderArticle 渲染 body 时识别「一、二、三、」中文序号段落 → `<h4 class="article-heading">`，≥2 个就生成目录（复用论文页 .article-toc 样式）；文章1无小节则 display:none 隐藏空卡。验证：文章2生成5项目录，文章1隐藏。
2. **图片灯箱**：通用轻量版，监听 .article-body 内 img 点击 → 全屏遮罩放大，Esc/点击关闭。零外部依赖，当前无图但基础设施就位。
3. **键盘快捷键**：`/` 聚焦搜索框（paperSearch/searchInput）、`←/→` 在文章页翻上下篇（读 #articleNav 链接）、Esc 关灯箱；输入框内不触发导航。
4. **阅读进度记忆**：localStorage 记每篇文章滚动比例（key=路径+query），重开自动恢复到上次位置；>95% 不恢复（读完不跳）；rAF 重试 30 帧覆盖异步渲染窗口。
5. **About 站点数据**：about.html 加 stats section（默认 hidden），renderAboutStats 计算 文章总数16/论文14/日常2/累计1.9万字/预计48分钟，填入 stats-grid。
6. **修 dark 模式正文暗坑**：.article-body color:#2e2e2e 在 dark 下无覆盖（几乎不可读），补 body[data-theme=dark] + @media 两套 color:#e8e8e8；.article-heading 同步补 dark。
- **代码块语法高亮**：暂缓。posts.json body 无 HTML 标签、无 ``` 围栏，论文页也无 <pre>/<code>，当前零载体，引 highlight.js 是无意义依赖。等写技术文章时再加。
- **styles.css 瘦身**：暂缓。12 个 !important 中 3 个是 prefers-reduced-motion（a11y 必须保留），9 个是上次 TOC 修复兜底——但 TOC 修复尚未线上验证，贸然移除有回归风险。等部署 v42 线上确认 TOC 正常后再清。
- 版本号：styles.css / script.js / page-transition.js 全部 v41→v42（26+7+26 个引用），确保本轮改动刷新缓存。
- 验证：node --check 通过；TOC 生成 + stats 计算用真实 posts.json 跑通。Edge headless 截图被沙箱 SIGKILL，未取得视觉图。

## 21:05 — Bug 扫描第三轮（安全 + 持久化）
发现并修复 3 个问题：
1. **飞书 App Secret 泄露（最严重）**：scripts/generate-papers.js 硬编码 FEISHU_APP_SECRET，git log -S 确认已进历史（7dec59c / b193773 / ed78372），仓库为公开 github.com/JUNNYOfficial/Blog。已改为 process.env 读取（缺失时报错退出），DOC_TOKEN 保留默认（低危）。**用户需去飞书开放平台重置 App Secret**；如要彻底清历史需 git filter-repo/BFG 重写。
2. **浅色模式不持久化**：initDarkMode 中选浅色时 setItem('theme','')，空字符串 falsy，重载后 !saved 又跟随系统偏好。修复：存 mode 原值（'dark'/'light'），判断改三态 `saved ? saved==='dark' : prefersDark`。
3. **公开导航暴露后台**：8 个公开页面 topbar 都有「管理」链接，已从 index/about/notes/daily/daily-posts/library/search/404 移除；admin/sync/import 内部页保留互链。admin.html 仍可 URL 直接访问（用户需收藏书签）。
- script.js 版本 v42→v43（7 个引用）。
- 验证：node --check 通过；grep 确认公开页面无 admin 链接残留。
- 其他排查结论（无问题）：posts.json 无重复 id、日期格式统一、内链全部有效（相对路径正确解析）、feed.xml/sitemap.xml XML 合法、论文页 prev/next 导航正常、tag 分类（日常/论文）与过滤逻辑一致。

## 21:20 — 切页过渡打字机效果
- page-transition.js 的 "zhilin Thinking…" 遮罩原本是静态文本，改为打字机动画：每个字符逐个浮现（40ms stagger + 260ms charIn），末尾闪烁光标（cursorBlink 0.75s）。
- buildMark() 在每次点击链接时重建节点，确保动画从头播放（否则页面加载时动画已播完）。
- dark 模式光标颜色补齐（#e5e5e5，两套选择器）。prefers-reduced-motion 下禁用动画。
- 版本：page-transition.js / styles.css v42→v43（各 26 个引用）。

## 21:25 — 搜索栏修复 + admin.js 残留 bug
**状态发现**：上一轮 0982e81 已落地所有 v=43 改进（密码散列、Token sessionStorage、Secret env、打字机等），用户随后 `git reset HEAD~1` 把那些改进丢回 working tree。git reflog 确认是用户主动 reset。
**本次操作**：先 ask 确认 `git reset --hard 0982e81` 恢复，再做本轮修复。

### Bug 修复
1. **搜索栏图标视觉混淆**（用户截图）：
   - .search-bar 从 flex+gap 改为 absolute 定位图标 + input padding-left: 52px，让 placeholder 第一个字"搜"与图标有明显视觉间距。
   - 图标改 18×18，颜色 #8a8a8a，pointer-events: none；隐藏所有 webkit-search 系列伪元素（消除 ✕ 与 placeholder 冲突）。
   - 移动端 padding 同步调整（12/16px、placeholder 起点 44px）。
   - styles.css v43→v44（26 个文件引用更新）。

2. **admin.js PASSWORD_KEY ReferenceError**（v=43 残 bug）：
   - 旧版 changePassword() 还用已删除的 PASSWORD_KEY 常量 → 调用必抛错。
   - 重写为"输入新密码 → 实时算 SHA-256 → 让用户复制哈希去替换源码里的 PASSWORD_HASH 常量"。密码不进任何持久化层。
   - admin.html 同步改按钮文案 + 加说明文案。

3. **admin.js fetch('script.js?v=23') 硬编码版本号**：
   - 改成 `fetch('script.js?t=' + Date.now(), { cache: 'no-store' })`，每次强制绕缓存拿最新。

4. **escapeHtml 缺单引号转义**：
   - 加 `&#39;` 转义单引号，防 `onclick="openEditor('${post.id}')"` 这种 JS 属性注入（post.id 若含 `'` 会逃逸属性）。
   - 新增 `escapeJsString(str)` 工具函数（转义 `\ ' " < > \n`），所有 onclick 字符串内插统一用它。

5. **publishToGitHub 仍读 localStorage 兜底 Token**：
   - v=43 改了 Token 存 sessionStorage，但 publish 路径 `token = sessionStorage || localStorage` 让旧 token 仍能 publish。
   - 改成**只读 sessionStorage**，旧 token 自动失效。
   - DOMContentLoaded 里多读一次 localStorage 仅为回填输入框（一次 UI 显示），并给旧用户显示迁移提示。

### 安全 meta（admin/sync/import 三页）
- 加 `<meta http-equiv="X-Frame-Options" content="DENY">` 防 clickjacking。
- 加 `<meta http-equiv="Referrer-Policy" content="no-referrer">` 防 referrer 泄漏。
- admin.html 多加 `<meta http-equiv="Permissions-Policy" content="geolocation=(), microphone=(), camera=()">`。
- 不加 CSP：用了 cdn.jsdelivr.net (lz-string) + cdnjs.cloudflare.com (qrcode) 两个外源，配 CSP 易踩坑；下次可单独评估 inline-only 方案。

### 版本号
- styles.css v43→v44（26 文件）
- admin.js v41→v44（admin.html v41 一直未升，0982e81 commit message 写了 v43 但 admin.html 的引用忘改，是连漏两轮的 bug）
- script.js / page-transition.js 维持 v43（本轮无改动）

### 验证
- node --check admin.js 通过。
- 26 个 HTML 的 styles.css 引用全部 v44；admin.html 的 admin.js 引用 v44。
- escapeJsString 已应用到三处 onclick 拼接（renderArticlesTable ×2、renderPapersTable ×1）。

### 用户待办（积压）
- ⚠️ **去飞书开放平台重置 App Secret**（旧值还在 3 个 git 历史 commit 里：7dec59c/b193773/ed78372，仓库公开）——飞书同步功能已整体删除，但历史 commit 里的 Secret 仍需重置
- 部署 v45 验证搜索栏放大镜不再与 placeholder "搜" 字混淆；验证 admin.html 密码修改新流程；验证改密码后 admin.js?v=45 的硬刷新能正常登入新哈希。

## 21:45 — 删除全部飞书同步功能

用户要求删掉所有和飞书同步的功能。

**删除的文件（4 个）：**
- `scripts/sync-feishu.js` — 飞书 API 同步脚本
- `scripts/generate-papers.js` — 飞书论文页面生成脚本
- `.github/workflows/sync-feishu.yml` — GitHub Actions 自动同步工作流
- `飞书论文笔记写作规范.md` — 飞书写作规范文档
- `scripts/` 目录已清空并删除

**修改的文件（7 个）：**
- `admin.js`：移除 `feishuPosts` 变量、`renderPapersTable()`、`filterPapers()`；`loadData()` 不再分离本地/飞书；`generateScriptJs()` 移除飞书论文导出段；`filterArticles()` 不再过滤 `f` 开头 ID
- `admin.html`：移除「论文管理」tab、飞书论文统计卡（statFeishu）、整个论文管理面板 section；admin.js 版本 v=44→v=45
- `import.html`：移除飞书论文计数显示
- `sync.html`：移除飞书论文计数显示
- `notes.html`：「自动同步」卡片改为「论文笔记」描述
- `script.js`：注释从「飞书论文」改为「论文」
- `README.md`：删除「飞书文档自动同步」整段；项目结构移除 scripts/ 和 sync-feishu.yml；维护说明更新

**保留的内容：**
- `papers/*.html`（14 篇已生成的静态论文页面）—— 这些是内容产物，不依赖飞书 API
- `posts.json` 中的论文数据（id 以 f 开头）—— 运行时仍正常加载渲染
- `sync.html` 本身（手机数据同步功能，与飞书无关）
- `robots.txt` 的 `Disallow: /sync.html`（sync.html 仍存在）

**版本号：** admin.js v=44→v=45（仅 admin.html 引用）。其他文件版本号不变。

## 21:55 — Bug 修复 + 安全加固 + 灯箱合并

用户说飞书后台程序已删除（Secret 泄露不再是问题），要求继续排查。

**修复 2 个真 bug：**
1. **`escapeHtml` 未定义**：`script.js` 的 `renderNotes()` 调用了 `escapeHtml(query)`（搜索无结果时显示用户输入），但该函数只在 `admin.js` 中定义。notes.html 搜索无结果时必抛 `ReferenceError`。→ 在 script.js 中补上 `escapeHtml()` 函数。
2. **about.html 站点数据不渲染**：about.html 有 `data-page="about"` 和 hidden 的 `#aboutStats` section，但不加载 script.js，`renderAboutStats()` 永远不执行。→ 补上 `<script src="script.js?v=44">`。

**安全加固：**
- `escapeHtml` 补上后，顺手把 script.js 所有 `innerHTML` 赋值中的文章元数据（title/summary/source/date/reading/tag）全部走 `escapeHtml()`，消除潜在 XSS 注入面（createArticleCard、renderDaily、renderDailyPosts、renderHome、renderArticle 正文/目录/推荐/上下篇导航、renderNotes 卡片/分类标题）。

**灯箱合并：**
- script.js 原有两个图片放大 overlay：`.img-overlay`（全局所有图片 >80px）和 `.lightbox-overlay`（仅 `.article-body` 内图片）。点击文章正文图片时两个都触发，叠在一起。→ 合并为一个 `.img-overlay`，保留全局捕获逻辑 + 补上 `currentSrc` 读取 + `aria-hidden` 状态切换；删除 `.lightbox-overlay` 的 JS 和 CSS。

**SEO：**
- article.html 补上静态 `og:url`、`og:image`、`og:site_name`、`twitter:card`（之前只有动态 JS 设置 og:url，爬虫看不到）。

**版本号：**
- script.js v=43→v=44（8 个 HTML 引用）
- styles.css v=44→v=45→v=46（26 个 HTML 引用，因删除 .lightbox-overlay CSS + 切页动画减速）
- page-transition.js v=43→v=44（26 个 HTML 引用，切页动画减速）
- admin.js 维持 v=45（上一轮已 bump）

## 22:00 — 切页动画减速

用户反馈"页面切换速度太快"。排查发现：打字机动画完成需 ~900ms（17 字符 × 40ms stagger + 260ms charIn），但页面在 600ms 就跳转了，动画被截断。

调整：
- 字符 stagger：40ms → 55ms（更从容的逐字节奏）
- charIn 动画：260ms → 300ms
- 遮罩淡入：450ms → 500ms
- 跳转延迟：600ms → 1300ms（动画在 ~1180ms 完成，留 ~120ms 呼吸后切走）
- 入场清理：700ms → 800ms

版本：page-transition.js v=43→v=44、styles.css v=45→v=46（各 26 引用）。

